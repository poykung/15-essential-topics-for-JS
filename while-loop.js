/*loops กลุ่มคำสั่งที่ใช้ในการวนรอบ (loop) โปรแกรมจะทำงานไปเรื่อยๆจนกว่าเงื่อนไขที่กำหนดจะเป็นเท็จ
-- while
-- for
-- do while


while loop ทำตามคำสั่งภายใน loop ไปเรื่อยๆเมื่อเงื่อนไขที่กำหนดเป็นจริง
โครงสร้าง while(เงื่อนไข) {
    คำสั่งทำซ้ำเมื่อเงื่อนไขเป็นจริง
}
*/

let count = 1
while(count <= 5){
    console.log("Say Hi")
    count++;
    if(count == 3){
        break;
    }
}