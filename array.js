/*Array ตัวแปรที่ใช้ในการเก็บข้อมูลที่มีลำดับต่อเนื่องกัน ซึ่งมีข้อมูลได้หลายค่าใช้หมายเลข index ในการจำแนกข้อมูลแต่ละตัว

การสร้าง array
ชื่อ array = [ข้อมูลที่อยู่ภายใน array ใช้เครื่องหมาย , ในการขั้นตำแหน่งของข้อมูล]
การเข้าถึงข้อมูลภายใน array ทำได้โดยการอ้างอิงหมายเลข index ในแต่ละช่องโดยจะเริ่มนับจากตำแหน่งที่ 0
*/

let colors = ['red','green','blue','white']

console.log(colors[1]) //การแสดงผล gereen